mybatis:
  type-aliases-package: com.haige.entity   # 起别名
  mapper-locations: /com/haige/repository/mapper/*.xml  # mapper映射文件的位置
server:
  port: 8080
spring:
  application:
    name: haige
  datasource:
    haige:
      type: com.alibaba.druid.pool.DruidDataSource
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://cdb-gbfpnwve.bj.tencentcdb.com:10226/haige?useSSL=false&characterEncoding=utf8
      username: root
      password: haige123
      initialSize: 5
      minIdle: 5
      maxActive: 20
      maxWait: 60000
      timeBetweenEvictionRunsMillis: 60000
      minEvictableIdleTimeMillis: 300000
      validationQuery: SELECT 1 FROM DUAL
      testWhileIdle: true
      testOnBorrow: false
      #exceptionSorter: false
      testOnReturn: false
      poolPreparedStatements: true
      #filters: stat,wall,log4j
      maxPoolPreparedStatementPerConnectionSize: 20
      connectionProperties: druid.stat.mergeSql=true;druid.stat.slowSqlMillis=500
      useGlobalDataSourceStat: true
pagehelper:
  auto-dialect: "true"
  reasonable: "true"
  support-methods-arguments: "true"
  params: "count=countSql"
logging:
  config: classpath:log4j2.yml
aliyun:
  sms:
    accessKeyId: "LTAI4Fhz9C1SMqgUTgJYPA2x"
    accessSecret: "cLh4tNX44oH3XQUHa6G2lnhPOhjPyc"
    templateCode: "SMS_176523402"
    signName: "安居在家"
